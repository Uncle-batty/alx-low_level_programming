#include "main.h"

/**
 * swap_int - A function to swop two int values
 * @a: input 1
 * @b: input 2
 * Return: swoped int
 */
void swap_int(int *a, int *b)
{
	int temp;

	temp = *a;
	*a = *b;
	*b = n;
}
