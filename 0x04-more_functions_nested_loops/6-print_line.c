#include "main.h"

/**
 * print_line - Prints a line of n size _
 * @n: var for lentgh
 * Return: no return
 */
void print_line(int n)
{
	int x;

	if (n > 0)
	{
		for (x = 0; a <= n; x++)
		{
			_putchar(95);
		}
	}
	_putchar('\n');
}
